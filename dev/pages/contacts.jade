extends ../templates/base

block content
	div(id="dealibre-app")
		router-view()
block scripts
	// Component
	script.
		new Vue({
			created: function() {
				DealibreApp.methods.checkAuthorized(function(isAuth) {
					if (!isAuth) {
						DealibreApp.methods.setHeader("contacts", "header-mobile--white", "black");
					} else {
						DealibreApp.methods.setHeader("contacts", "header-mobile--white header-mobile--aut", "black");
					}
				});
				DealibreApp.methods.showFooter();
			}
		});
